FINAL 25 — GLOBAL STORE OPEN/CLOSE

Added to SaaS Owner Admin Panel:
- 🔴 Tutup Semua Toko: stops every tenant bot while SaaS stays online.
- 🟢 Buka Semua Toko: automatically starts every valid tenant bot again.
- Global close state persists in data/saas.json via allStoresClosed.
- If SaaS restarts while globally closed, tenant recovery stays disabled until Owner opens all stores.
- Opening/closing does not touch tenant data or Telegram sessions.
- Existing backup system remains intact; store open/close state is persisted as SaaS data and can trigger a meaningful backup once.
- No other bot logic, checkout, Premium Emoji, Auto Comment/Jaseb, FS/NFS, seller pricing, or restore flow changed.


FINAL 26 HOTFIX — startup no longer waits for tenant recovery before launching SaaS control bot. Tenant recovery runs after SaaS polling is online, so a slow/broken tenant cannot leave the panel stuck at Starting. No tenant feature logic changed. Also cleaned intentional stop bookkeeping.
